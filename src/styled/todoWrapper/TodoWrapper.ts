import {styled} from 'styled-components';

const TodoWrapper = styled.div`
    padding: 0 0 0 40vw;
`

const TodoInputWrapper = styled(TodoWrapper)`
    padding: 10vh 0 0 40vw;
`

export {TodoWrapper, TodoInputWrapper}